spring:
  cloud:
    config:
      name: client
      label: master
      profile: test
      uri:
        - http://localhost:8094
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka/
spring:
  cloud:
    config:
      name: client
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: gitconfigserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka/
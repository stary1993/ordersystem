spring:
  cloud:
    config:
      name: configclient
      label: master
      discovery:
        enabled: true
        service-id: gitconfigserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8088/eureka/